<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.ChildMapper">

  <select id="findAllByUserId" parameterType="long" resultType="com.example.demo.vo.Child">
    SELECT
      id,
      user_id AS userId,
      name,
      birth_date AS birthDate,
      gender,
      note,
      reg_date AS regDate,
      update_date AS updateDate
    FROM children
    WHERE user_id = #{userId}
    ORDER BY id DESC
  </select>

  <select id="findByIdAndUserId" resultType="com.example.demo.vo.Child">
    SELECT
      id,
      user_id AS userId,
      name,
      birth_date AS birthDate,
      gender,
      note,
      reg_date AS regDate,
      update_date AS updateDate
    FROM children
    WHERE id = #{id}
      AND user_id = #{userId}
  </select>

  <insert id="insert" parameterType="com.example.demo.vo.Child">
    INSERT INTO children(user_id, name, birth_date, gender, note)
    VALUES(#{userId}, #{name}, #{birthDate}, #{gender}, #{note})
  </insert>

  <update id="update" parameterType="com.example.demo.vo.Child">
    UPDATE children
    SET
      name = #{name},
      birth_date = #{birthDate},
      gender = #{gender},
      note = #{note}
    WHERE id = #{id}
      AND user_id = #{userId}
  </update>

  <delete id="deleteByIdAndUserId">
    DELETE FROM children
    WHERE id = #{id}
      AND user_id = #{userId}
  </delete>

</mapper>
